# 轻食计 · 产品需求文档（PRD）

> 本文档为「轻食计·减肥饮食助手」的交接文档。任何接手开发的人（或新的 AI agent），请**先完整读完本文档再动手**，避免重蹈"中途换人、上下文丢失、改乱已能用的功能"的覆辙。
>
> 最后更新：2026-09-19

---

## 一、产品定位

- **是什么**：一个减肥饮食记录与营养管理的**前端网页应用**。
- **不是什么**：不是手机 App（不需要安装）、不是独立的聊天机器人（AI 问答只是其中一个功能模块）。
- **用途**：
  1. 自用：记录每日饮食、计算热量与营养、获得减脂建议；
  2. 求职作品集：作为前端项目展示在 GitHub。
- **用户本人**：无技术背景。能描述要什么效果，但不维护代码——所有技术操作由开发方/AI 完成。

---

## 二、核心功能清单（已全部实现并验证）

### 1. 今日概况（首页）
- 今日热量环形进度：已摄入 / 目标 / 剩余可摄入
- 蛋白质、脂肪、碳水化合物、膳食纤维 四项达标进度
- 今日餐次汇总（早/午/晚/加餐，各餐热量）
- 首页三个大按钮：快速记录、记录饮食、查食物热量（手机端为全宽大按钮竖排）
- 问候语随时间变化；提示语随饱腹度变化（"有点饿了，记得吃够营养"）

### 2. 虚拟角色形象
- 男生 / 女生两种形象，可随时切换
- 每种形象 4 种状态：正常 / 饱腹 / 饥饿 / 活力满满
- 角色随用户身高体重（BMI）和当天饮食饱腹度**自动切换素材与表情**
- 素材为 AI 生成的卡通立绘（512×683 透明 PNG，已压缩）

### 3. 食物库
- 内置 112 种常见食物，分类：主食/肉蛋/水产/蔬菜/水果/豆奶/坚果/零食饮料
- 每个食物标注：每 100g 热量、蛋白质、脂肪、碳水、膳食纤维、优点、注意、建议、减脂友好度评分
- 支持搜索、按分类筛选
- **自定义食物**：用户可添加自己的食物；**常用食物自动排在前面**
- 食物卡片点击可直接加入饮食记录

### 4. 饮食记录
- 按日期记录（可前一天/后一天切换）
- 按四餐：早餐 / 午餐 / 晚餐 / 加餐
- 支持修改分量、删除
- 自动汇总全天热量与营养素
- **关键业务规则：食品按"成品整份"记录，不按散装克重**。例如板烧鸡腿堡按整份 400 千卡 / 200g 记录，而不是让用户按克购买。营养快照在记录时固化（kcal100/protein100/fat100/carbs100），避免后续食物数据修改影响历史记录。

### 5. 智能助手（AI 对话）
- **人设**：知识丰富的营养学者/语言学家。回答结构固定为：
  1. 先简短直接回答问题；
  2. 给出饮食/减脂建议；
  3. 对食物按 10 分制做营养评分；
  4. 与饮食无关的问题，一律回答"不太清楚/不知道"，不做网页外的闲聊。
- **双层回答机制**：
  - 本地规则引擎（离线可用）：食物热量查询、食物对比、饮食推荐、今日状态核对、运动消耗、减脂知识，全部基于内置食物库与用户档案计算；
  - 联网大模型（可选）：本地规则答不上时，经后端代理调用 DeepSeek API 扩展回答。
- **AI 回答中的食物一键添加**：AI 提到的食物可直接选择「添加到今日记录」或「存入食物库」，营养数据自动填好。
- **离线降级**：单文件 HTML 离线打开时，AI 请求失败自动降级为本地规则回答。

### 6. 我的方案
- 填写身高、体重、性别、年龄、活动水平、减脂目标
- 自动计算 BMR、TDEE、每日目标热量、三大营养素目标克数

### 7. 运动消耗
- 运动种类（MET 值）+ 时长 → 消耗热量计算

### 8. 界面与视觉
- 主色调：**蒂芙尼蓝 / 蓝绿色**，高级简约风
- 背景：ReactBits Iridescence WebGL 珠光流光效果（深青绿底色）
- 深色侧边栏 + 白色内容浮层
- **响应式**：桌面端侧边栏；移动端自动切换为顶部汉堡抽屉菜单 + 全宽大按钮

### 9. 两种运行形态
- **开发版**：`npm run dev:all`，前端 32111 端口 + AI 后端 8787 端口，功能完整（含联网 AI）
- **单文件版**：构建产物 `轻食计-单文件版.html`（约 1.5MB），双击即可在浏览器打开，无需安装任何环境；数据存浏览器本地；联网 AI 自动降级为本地规则

---

## 三、技术架构（接手必读）

| 项 | 内容 |
|---|---|
| 框架 | React 19 + TypeScript |
| 构建工具 | Vite 8 |
| 样式 | Tailwind CSS v4 + shadcn/ui + lucide-react 图标 |
| 路由 | react-router-dom，**必须用 HashRouter**（单文件 file:// 双击打开时 BrowserRouter 会导致导航失效） |
| 图表 | echarts-for-react |
| 前端数据存储 | 浏览器 localStorage（键前缀 `diet-assistant:`），无后端数据库 |
| AI 后端 | `server.js`（Express，8787 端口）：浏览器请求 `/api/chat` → 转发到 DeepSeek API。**API Key 只在服务端环境变量，绝不出现在前端代码或仓库中** |
| 单文件打包 | `vite-plugin-singlefile`：把所有 JS/CSS/图片内联进一个 index.html |

**关键安全规则（已验证，不可破坏）**：
- `.env`（含真实 DEEPSEEK_API_KEY）必须永远在 `.gitignore` 中，不得提交到 GitHub；
- 仓库中只允许存在 `.env.example` 模板；
- 已做过全仓库扫描确认无任何硬编码 key。

---

## 四、本地运行

```bash
cd 工程根目录
npm install        # 首次
npm run dev:all    # 前端(32111) + AI 后端(8787)
npm run build      # 构建单文件版到 dist/output/index.html
npm run lint       # 类型检查 + ESLint
```

AI 配置：复制 `.env.example` 为 `.env`，填入 `DEEPSEEK_API_KEY=sk-你的key`。

手机访问：电脑与手机同一 WiFi，手机浏览器打开 `http://<电脑局域网IP>:32111`。

---

## 五、工程路径与仓库信息

| 项 | 位置 |
|---|---|
| 主工程（git 仓库） | `C:\Users\qzj\DoubaoWork\chats\2026-09-15\new-chat-1\diet-assistant` |
| 桌面归档 | `C:\Users\qzj\Desktop\减脂网站\`（1-项目源码 / 2-角色素材 / 3-项目文档 / 单文件版.html） |
| GitHub 仓库 | https://github.com/qqqzj69/diet-assistant （master 分支） |
| 推送注意 | git 在 `C:\Program Files\Git\cmd`；推送需设置代理 `HTTP_PROXY=http://127.0.0.1:10808` |

**数据独立性提醒**：开发版（localhost:32111）与单文件版（file://）的 localStorage 互相隔离，数据不互通。

---

## 六、未完成 / 待办（接手可按此继续）

1. **部署上线**：将项目免费部署到 Vercel/Netlify，拿到可分享的在线网址，填入 README 的"在线预览"位置。（仓库目前待改为 Public）
2. **数据导出/备份功能**：建议加一个"导出记录为文件"按钮，防止浏览器清数据丢失记录。
3. 单文件版与开发版的 AI 能力差异：单文件版为离线规则版，联网 AI 仅开发版可用。

---

## 七、交接注意事项（最重要的一节）

1. **用户没有技术背景**：不要假设他懂 React/Vite/路由等概念。需求用"效果"描述（如"按钮变大、手机上竖着排"），不要用技术词提问。
2. **先定形态再动手**：这个项目的所有混乱都源于中途形态摇摆（app/网页/智能体反复横跳）。当前确定形态：**网页**。如需改变形态，先跟用户确认再动代码。
3. **每完成一个小功能就提交一次**（git commit + push），提交信息写清楚这次改了什么。改坏了要能回退——这是这个项目唯一可靠的备份手段。
4. **不要中途换人/换工具栈**：如需更换执行方，必须先更新本文档，把"做到哪、已知 bug、下一步"写进去。
5. **改动前先读现有代码**：历史上出现过"改一处坏三处"的问题（营养计算、浮点精度、记录快照逻辑都踩过坑）。
6. **用户明确拒绝过**：付费服务器、域名、上线发布（除非再次明确要求）。

---

## 八、需求变更记录

| 日期 | 变更 |
|---|---|
| 初期 | 减肥计算工具雏形 |
| 中期 | 加入男女 3D 角色 × 4 状态；改为 AI 营养学者人设；食品按成品整份记录；珠光背景；手机适配 |
| 后期 | 单文件 HTML 版（HashRouter）；角色素材压缩；求职作品集 README |
